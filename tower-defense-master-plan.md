# 《终端协议》开发总纲

## 0. 这份文档是什么

这是一份给开发 agent 看的规划和手册，目的是让 agent 不需要反复回来问设计问题，就能沿着这份文档把游戏从零做到能玩。文档分两部分：前面是系统设计，规定游戏各个模块该怎么运作；后面是开发路线图，规定按什么顺序做、每一步做完要检查什么。

文档里没有覆盖到的细节，比如某个塔的具体伤害数值、某一关的具体地图形状，由 agent 在实现时按文档给出的框架自行决定，不需要为这些细节停下来确认。真正需要确认的，只有文档末尾"开放问题"里列出的那几项。

游戏定名为《终端协议》，英文名 Terminal Protocol。

## 1. 游戏总览

这是一款科幻题材的单机塔防游戏，用一个 HTML 文件承载，在浏览器里直接运行，不需要安装任何东西，也不连接服务器。玩法核心是经典塔防：敌人沿一条固定路线前进，玩家在路的两侧放置和升级防御塔来拦截敌人，守住终点。

整体规模是一个完整战役：三十关以上，划分成六个章节，每章末尾有一个 Boss 关。除了单局内的战斗，游戏还有局外的科技树，把玩家在多局游戏里积累的进度串联起来，形成长线的成长感。

主要在电脑上用鼠标操作，界面和剧情提供中文和英文两个版本，可以在设置里切换。难度分简单、普通、困难三档，通关整个战役后解锁无尽模式。

## 2. 世界观与叙事基调

故事背景是一个失控的人工智能，战场设定在虚拟的网络空间里。玩家扮演一套防御协议，任务是在这个人工智能的入侵浪潮里守住一个个网络节点，防止它蔓延到核心系统。

每个敌人单位在设定上是失控人工智能派出的恶意进程，防御塔是玩家部署的安全协议或者防火墙模块，资源则对应可调用的算力。这样一套对应关系能让所有系统名词都落在同一个世界观里，不需要另外发明一套无关的道具体系。

剧情基调是严肃紧张的科幻悬疑，不走轻松幽默的路线。游戏有完整的世界观设定和角色对话，章节开头结尾有叙事过场，关卡进行中也会插入对白，比如敌人科技树解锁到某个节点、或者某一关快要失败时触发几句警告性的台词。

具体的对话文本和世界观细节由 agent 在实现时创作，只要符合"严肃科幻悬疑"这个基调，不需要每一句都拿来确认。防御协议的代号、失控人工智能的名字这些命名，agent 可以自行拟定。

## 3. 技术架构与开发方式

### 3.1 运行形态

最终交付物是一个单独的 HTML 文件，用原生 JavaScript 加 Canvas 实现，不依赖任何外部库或者 CDN。Canvas 是浏览器里可以用代码逐帧绘制图形的一块区域，游戏画面、特效、UI 都通过它绘制。声音用浏览器自带的 Web Audio 功能直接用代码合成，不需要准备任何音频文件。

### 3.2 开发期的文件组织

由于代码量会很大，开发阶段不建议把所有内容堆在一个文件里改。建议按下面这样拆分源文件：

- `core/` 目录放游戏循环、状态管理、输入处理这些引擎层代码
- `entities/` 目录放塔、敌人、弹幕这些游戏对象的逻辑
- `systems/` 目录放波次系统、经济系统、科技树、存档这些独立系统
- `content/` 目录放关卡数据、塔和敌人的数值表、剧情文本，这部分数据量最大，也最需要独立维护
- `render/` 目录放画面绘制和特效相关代码
- `audio/` 目录放 Web Audio 声音合成代码
- `i18n/` 目录放中英文文本
- `main.js` 作为入口，把上面各部分串起来

开发完成后，用一个构建脚本把这些源文件按依赖顺序拼接、内联进一个 HTML 文件里，生成最终可以直接双击打开的产物。这个构建脚本本身也是项目的一部分，agent 需要在项目初始化阶段就把它写好，后续每次改动源码后跑一遍就能得到最新的单文件版本。

### 3.3 数据驱动的内容组织

塔、敌人、关卡、剧情文本这些内容量大且会反复调整的部分，都应该用数据表的形式组织，比如一个塔类型对应一条配置记录，而不是为每个塔单独写一段绘制和逻辑代码。这样后续调整数值、增删关卡内容的时候，改数据就够了，不需要动核心逻辑。

## 4. 核心玩法系统

### 4.1 地图与路径系统

每一关的地图上有一条固定的路线，敌人从起点沿这条路线走到终点，玩家不能在路上放塔，只能把塔放在路线两侧的空地上。地图用网格或者自由坐标定义路径点，路径点之间用直线或者曲线连接，敌人按路径插值移动。

不同关卡可以用不同形状的路线制造变化，比如直线、折线、螺旋、交叉，但同一关内路线是唯一且固定的，不涉及分叉或者敌人绕路的逻辑，这样能省掉寻路算法的复杂度。

### 4.2 防御塔系统

塔一共分四个流派：能量、爆破、电磁、支援。每个流派下设三种基础塔，一共十二种基础塔，再加上后续可能随章节解锁的额外流派或塔型。

四个流派的定位建议如下：

- 能量流派：单体高精度攻击，伤害集中在一个目标上，适合应对高防御的单体敌人
- 爆破流派：范围伤害，一次攻击能命中路径上一段范围内的多个敌人，适合应对成群的敌人
- 电磁流派：控制和减益，比如减速、连锁禁用护盾、大范围电磁脉冲让隐身敌人现形，自身直接伤害不高，但能让其他塔发挥得更好
- 支援流派：不直接攻击，提供增益，比如给周围的塔加攻速、加范围侦测、回收部分资源、或者主动技能

每种基础塔有独立的升级路径，升级分两层：关卡内的临时升级用当局获得的资源购买，通关或者失败后不保留；局外的永久科技树用跨局积累的科技点解锁，一旦解锁就永久生效，影响这个塔在所有后续关卡里的初始强度或者解锁额外形态。

具体每种塔的伤害数值、攻击间隔、升级消耗，由 agent 在实现时先给出一版初始数值，后续通过实际游玩来调整平衡性，不需要提前定死。

### 4.3 敌人系统

敌人分基础类型和特殊能力两层。基础类型至少包含快速型、高护甲型、成群型这三种，分别对应不同流派塔的克制关系。

在基础类型之上，敌人可以携带特殊能力，包括护盾（先消耗护盾值才能造成实际伤害）、治疗（持续恢复自身或者周围友军血量）、隐身（普通塔看不到，需要电磁流派的侦测能力才能锁定）、分裂（死亡时分裂成若干个更弱的子单位）。这些能力可以单独出现，也可以叠加在同一个敌人身上，用来在后期关卡里制造更复杂的应对局面。

每个章节末尾是一个 Boss 关，Boss 是这个章节主题的强化版本，血量和体型明显更大，通常还会有一到两个专属机制，比如召唤小怪、阶段性免疫某个流派的伤害、或者对场上的塔造成范围干扰。六个章节对应六个 Boss，每个 Boss 的具体机制由 agent 结合章节主题设计。

### 4.4 资源与经济系统

关卡内只使用一种资源，用来建造新塔和购买临时升级。资源通过击杀敌人获得，具体产出数值和塔的造价、升级花费需要保持整体经济曲线合理，即前期资源紧张、后期随着通关进度和科技树加成逐渐宽松。

### 4.5 波次系统

每一波敌人的出现方式支持两种，并且可以互相切换：一种是玩家手动点击按钮提前召唤下一波，用来主动控制节奏、避免资源浪费；另一种是按倒计时自动刷新。除此之外还提供播放倍速功能，让玩家可以加速已经熟悉的低强度波次，减少重复等待的时间。

### 4.6 关卡与章节结构

整个战役分六个章节，每章大约五到六关，加起来三十关以上，最后一关是每章的 Boss 关。章节之间在美术和敌人风格上做出区分，配合世界观里"失控人工智能不断扩张"的设定，可以让每一章代表这个人工智能占领的一个新区域或者演化出的一种新形态。

具体每一关的地图布局、敌人配置、资源和波次的数值曲线，由 agent 在内容生产阶段批量设计，遵循难度随关卡推进逐步上升的整体原则。

### 4.7 难度与无尽模式

游戏提供简单、普通、困难三档难度，影响敌人的血量、速度、资源产出这些数值倍率，不改变关卡结构本身。通关整个三十多关的战役后解锁无尽模式，无尽模式在最后一关的地图基础上无限刷新越来越强的波次，用来给通关后的玩家提供额外的挑战目标。

## 5. 成长与存档系统

局外的科技树是一张解锁图，前面的节点研究完，后面的节点才能解锁。科技树的节点对应塔的永久强化、新塔型的解锁、或者全局性的增益，比如提高所有关卡的初始资源。科技点通过通关关卡获得，具体获得数量可以和关卡星级或者完成质量挂钩，鼓励玩家不只是勉强通关，还有动力去打得更好。

存档同时支持两种方式：默认自动保存在浏览器本地，关闭页面重新打开后进度还在；同时提供导出和导入存档文件的功能，让玩家可以把存档备份出来，或者换一台电脑继续玩。存档需要包含关卡通关进度、科技树解锁状态、当前难度和语言设置。

## 6. 表现层

### 6.1 视觉风格

整体画面走霓虹发光的线条和粒子特效风格，塔、敌人、弹幕、地图元素都用发光线条和几何形状构成，符合"虚拟网络空间"这个设定。不同流派的塔可以用不同的主色调区分，比如能量流派用青色、爆破流派用橙红色、电磁流派用紫色、支援流派用绿色，方便玩家在战斗中快速识别。

### 6.2 音频

音效和背景音乐都通过 Web Audio 用代码合成，不需要准备任何外部音频素材。音效至少覆盖建塔、升级、攻击、敌人死亡、波次开始、关卡胜利和失败这几个关键节点。背景音乐可以用简单的合成器音色循环编写，不同章节可以在节奏或者音色上做一些区分，避免全程只有一首曲子。

## 7. 本地化与设置

界面文本、剧情对话、教程提示都需要准备中文和英文两个版本，通过设置里的语言选项切换，切换后立即生效，不需要重启游戏。文本内容建议统一放在 `i18n/` 目录下的语言文件里，用一个统一的文本键值系统管理，方便后续增删或者调整。

## 8. 新手引导

新手引导安排在第一章，用逐步教学的方式展开，每次只介绍一个新东西，比如第一关只教怎么放塔，第二关再引入升级机制，第三关引入资源管理，以此类推，避免一次性把所有系统都塞给玩家。引导内容可以用高亮提示、暂停弹窗或者角色对白的形式呈现，具体形式由 agent 结合已经定好的剧情对话系统来实现，不需要单独做一套引导 UI。

## 9. 开发路线图

路线图按阶段推进，每个阶段结束后先由 agent 自动验证，再交给 Yuyuan 试玩确认，确认通过后再进入下一阶段。自动验证的方式是用无界面浏览器，也就是没有窗口的浏览器程序，在后台自动打开构建出的 HTML 文件、模拟点击关键按钮、截图检查画面是否符合预期。

**阶段零：项目初始化**
搭建第 3 节里描述的文件结构，写好构建脚本，做出一个能显示空白 Canvas 画布的最小可运行版本。验证标准是构建脚本能正常合并出一个 HTML 文件，并且这个文件能在浏览器里正常打开、没有报错。

**阶段一：核心战斗循环**
实现地图路径绘制、一种最基础的塔、一种最基础的敌人、敌人沿路径移动、塔自动攻击范围内的敌人、敌人死亡掉血逻辑、敌人到达终点扣血、血量归零判定失败、清空所有波次判定胜利。这一阶段做完，游戏应该已经是一个能打通一整关的最小可玩版本，只是内容非常单薄。

**阶段二：塔与流派完整实现**
把十二种基础塔和四个流派的机制全部实现，包括临时升级和对应的科技树节点占位。这一阶段暂时可以用简单的几何图形代表每种塔，视觉打磨放到后面阶段。

**阶段三：敌人多样性与 Boss**
实现护盾、治疗、隐身、分裂这几种特殊能力，以及至少一个完整的 Boss 战作为验证案例。

**阶段四：资源、波次与关卡系统**
完整实现经济系统的资源产出和消耗、手动加自动两种波次触发方式、倍速播放，并且做出支持多关卡切换的关卡选择结构。这一阶段结束时，游戏应该能从关卡选择界面进入任意一关并正常游玩。

**阶段五：局外成长系统**
实现完整的科技树解锁逻辑、本地存档的自动读写、存档的导出导入功能。

**阶段六：视觉与音频打磨**
把阶段二到阶段四里用简单图形代表的内容，替换成霓虹发光风格的正式美术效果，并接入 Web Audio 音效和背景音乐。

**阶段七：剧情与本地化**
实现对话系统、章节过场、中英文文本切换，把已经想好的世界观和角色对白正式填充进去。

**阶段八：难度系统与无尽模式**
实现三档难度对数值的影响，以及通关后解锁的无尽模式。

**阶段九：新手引导**
在第一章关卡里插入分步教学提示。

**阶段十：全量内容生产**
批量制作三十多关的具体关卡内容，包括每关的地图、敌人配置、资源曲线，并且完成六个章节的整体难度爬升曲线设计。

**阶段十一：打磨与验收**
修复前面阶段积累的问题，检查整体性能，跑一遍完整的无界面浏览器回归测试，确认从开局到通关全流程都能顺利跑完，再交付最终的单文件版本。

## 10. 验证与测试流程

每个阶段完成后，agent 先用无界面浏览器自动跑一遍关键流程并截图，检查画面渲染、按钮响应、状态切换是否符合这一阶段的预期，如果发现明显问题就自行修正后再重新截图确认。自动检查通过后，把这一阶段的构建产物交给 Yuyuan 试玩，收到反馈后再决定是否需要调整，还是可以直接进入下一阶段。

如果某一步遇到数据缺失或者局部功能跑不通这类小障碍，不需要停下来专门诊断并等待确认，先带着已有的实现继续往后做，把这个缺口在阶段总结里标注出来，后续统一处理。

## 11. 开放问题

以下几项目前还没有确定，agent 可以先用文档里给出的默认方案往下做，如果 Yuyuan 后续有具体想法，再回来调整：

- 六个章节各自的具体主题和视觉区分方案
- 每个 Boss 的具体机制设计
- 十二种基础塔和后续科技树节点的具体数值表
